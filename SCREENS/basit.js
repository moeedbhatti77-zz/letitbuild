import React, { Component } from "react";
import {
  Button,
  View,
  Text,
  ScrollView,
  FlatList,
  TouchableOpacity,
  Alert,
  StyleSheet,
  Image,
  TextInput,
  ImageBackground,
  flex,
  SafeAreaView,
} from "react-native";

export function FEX(props, route) {
  return (
    <SafeAreaView style={{ flex: 1, marginTop: 40 }}>
      <Text> HELLO </Text>
    </SafeAreaView>
  );
}
